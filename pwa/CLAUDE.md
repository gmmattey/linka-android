# PWA SignallQ — Instruções para Agentes

## Projeto

SignallQ PWA — app web progressivo de diagnóstico de conectividade.

## Agente responsável

**Renan** — qualquer implementação, revisão ou correção de código nesta pasta.

## Stack

- React + TypeScript + Vite
- Mesmo padrão do `SignallQ Admin/` (referência de setup)

## Skills aplicáveis

- `react-typescript-check` — revisão de código React/TypeScript
- `pwa-platform-rules` — limitações de plataforma web
- `browser-limitations` — restrições de APIs de navegador
- `android-pwa-parity` — paridade de comportamento entre Android e PWA
- `linka-design` — design system SignallQ (tokens, Material 3, cores)

## Identificadores técnicos

Nenhum identificador técnico reservado desta pasta ainda. Quando o projeto for inicializado, registrar aqui.

## Rastreamento

Linear: projeto `SignallQ` (SIG), buscar issues com label `area:pwa` ou equivalente.

## Restrições

- Não alterar arquivos fora de `pwa/` sem aprovação
- Não criar agentes novos sem alinhamento com Claudete
- Não commitar sem revisão do Renan ou Gema
