.gradle/
.kotlin/
.idea/
.wrangler/
.dart_tool/
.impeccable/

build/
**/build/
!scripts/build/
!scripts/build/**
out/
tmp/
captures/
coverage/

local.properties
key.properties
*.jks
*.keystore
*.crt
*.key
play-service-account*.json
play-publisher*.json
.env
.env.*
!.env.example
.dev.vars
!key.properties.template

node_modules/
__pycache__/
*.pyc

*.iml
*.log
*.zip
*.apk
*.aab

# Segredos e certificados locais (apenas o conteúdo — o diretório é rastreado)
segredos/*
!segredos/.gitkeep
certificados/*
!certificados/.gitkeep

# Issues locais migradas (mantidas como histórico opcional — descomente para ignorar)
# .issues/

# Tasks/queue dos agentes (estado local)
.claude/tasks/

# Logs e artefatos gerados (build/upload) — nunca versionar
build_log.txt
firebase_upload.txt
*_log.txt
tests/prints/TEMP.md

# Índice de memória gerado pelo harness (grande, não versionável)
memory-index.json

# Design system — artefatos gerados pelo converter (nunca versionar)
packages/design-system/ds-bundle/
packages/design-system/.design-sync/.cache/
packages/design-system/.design-sync/learnings/
packages/design-system/.design-sync/node_modules/
packages/design-system/.ds-sync/
packages/design-system/dist/
packages/design-system/package-lock.json
integrations/cloudflare/signallq-admin-worker/dist/

# Claude Code runtime lock files
**/.claude/scheduled_tasks.lock
